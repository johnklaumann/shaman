## What

## Why

## Verified

- [ ] `npm test` green
- [ ] `node scripts/build-adapters.mjs --check` (if rules changed: regenerated adapters committed)
- [ ] `node bench/gate-bench.mjs --ci` (if lexicons/thresholds changed)
- [ ] No new runtime dependencies
- [ ] Benchmark numbers, if any, come from committed scripts — not typed by hand
