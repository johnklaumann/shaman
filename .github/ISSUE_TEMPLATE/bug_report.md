---
name: Bug report
about: Something broke — a hook, the gate, the CLI, a benchmark
labels: bug
---

**What happened**

**What you expected**

**Repro**
- Tool + version (Claude Code / Codex / CLI): `claude --version` or equivalent
- OS + Node version: `node --version`
- Shaman version: `npm ls -g shaman-ai` or plugin version
- Steps / the exact prompt (if gate-related)

**Gate-related?** Paste the output of:

```
npx shaman-ai score "the prompt that behaved wrong"
```

**Logs / output**
